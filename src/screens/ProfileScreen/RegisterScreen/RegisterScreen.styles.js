import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    Image: {
        resizeMode: "contain",
        height: 150,
        width: "100%",
        marginTop: 100,

    },
    content: {
        marginHorizontal: 40,
        paddingBottom: 40,
    }
});